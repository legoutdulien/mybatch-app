# Secrets
.env
.env.*
!.env.example

# Dossiers de migration locaux (pas dans le repo)
airtable-export/
photos-temp/

# Backup local des fichiers d'origine (sauvegarde, pas a deployer)
backup-originaux/

# Editeurs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Node modules (pas utilises ici mais au cas ou)
node_modules/

# Tests / debug
.netlify/

# Repere local "quelle app" (note interne, ne pas publier)
_LISEZMOI-QUELLE-APP.txt

# Doc interne de reprise — NE JAMAIS publier (contient IDs/infos sensibles)
HANDOFF.md
